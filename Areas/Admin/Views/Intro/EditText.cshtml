@model StaticPage
@{
	ViewData["Title"] = "Ändra text";
}
<div class="col-sm-9">
	<h2>@ViewData["Title"]</h2>
	<hr />
	<form method="post" asp-action="EditText" class="form-horizontal col-md-12" role="form">
		<div asp-validation-summary="All" class="text-error"></div>
		<input asp-for="Id" type="hidden" />
		<div class="form-group">
			<label for="Name">Namn</label>
			<input type="text" class="form-control" id="Namn" placeholder="Namn" asp-for="Name">
		</div>
		<div class="form-group">
			<textarea asp-for="Text1"></textarea>
		</div>
		<br>
		<div class="form-group">
			<a asp-action="Index" class="btn btn-default" role="button">Tillbaka till listan</a>
			<button type="submit" class="btn btn-primary">
				<i class="fa fa-floppy-o fa-lg"></i>&nbsp; Spara
			</button>
			<a asp-action="DeleteText" asp-route-id="@Model.Id" class="btn btn-danger">
				<i class="fa fa-trash-o fa-lg"></i>&nbsp; Radera</a>
		</div>
	</form>
</div>

@section Scripts {
	@{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
	@{ await Html.RenderPartialAsync("_TinyMCE"); }
}