@model IEnumerable<Stadga>
@{
	ViewData["Title"] = "Stadga";
}
<div class="col-sm-9">
	<h2>@ViewData["Title"]
		<span class="pull-right">
			<a asp-action="Create" class="btn btn-default">Skapa ny stadga</a>
		</span>
	</h2>
	@if (TempData.ContainsKey("Message"))
	{
		<div class="alert alert-info alert-fixed alert-dismissable" role="alert">
			<a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
			<strong>@TempData["Message"]</strong>
		</div>
	}

	<br>
	<table class="table table-striped table-condensed table-hover table-row-click col-md-12">
		<thead>
			<tr>
				<th>
					@Html.DisplayNameFor(model => model.Number)
				</th>
				<th>
					@Html.DisplayNameFor(model => model.Name)
				</th>
				<th></th>
			</tr>
		</thead>
		<tbody>
			@foreach (var item in Model)
			{
				<tr>
					<td>
						@Html.DisplayFor(modelItem => item.Number)
					</td>
					<td>
						@Html.DisplayFor(modelItem => item.Name)
					</td>
					<td>
						<a asp-action="Edit" asp-route-id="@item.Id">Redigera</a>
					</td>
				</tr>
			}
		</tbody>
	</table>
</div>